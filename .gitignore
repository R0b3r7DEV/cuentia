# ── Symfony / PHP ──
/backend/vendor/
/backend/var/
/backend/.env.local
/backend/.env.*.local
/backend/public/bundles/

# ── Node / React ──
/frontend/node_modules/
/frontend/dist/
/frontend/.env.local

# ── Env secrets (only *.local; committed .env holds non-secret defaults) ──
# ES: solo *.local; el .env commiteado tiene valores por defecto no secretos
.env.local
.env.*.local

# ── Editor / OS ──
.idea/
.vscode/
.DS_Store
Thumbs.db
